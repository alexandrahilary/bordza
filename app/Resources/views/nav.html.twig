{% extends 'base.html.twig' %}
{% block body %}
{% block nav %}

<div class="container-fluid" id="menu">
            <div class="row justify-content-center">
              <div class="col-12 row justify-content-center">
                <img style="width:70px;height:70px;margin:20px"  src="{{ asset('/assets/images/logo/bordza-blanc.png') }}">
              </div>
              <div class="col-12 row justify-content-center">
                <span style="color:white;font-weight:bold;font-size:2em">BORDZA</span>
              </div>
              {% if app.user %}
              <div class="col-12 row justify-content-center">
                <span style="color:white;font-weight:bold;font-size:2em">Bonjour {{ app.user.username | capitalize }}</span>
              </div>
            </div>
</div>
<div class="container-fluid sticky-top" id="nav">
            <div class="row justify-content-center">
              <div class="col-12">
                <nav class="navbar navbar-expand-lg justify-content-center">
                  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navMenu" aria-controls="navMenu" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                  </button>
                  
                  <div class="collapse navbar-collapse justify-content-center" id="navMenu">
                    <ul class="navbar-nav">

                      {% if app.user.hasRole('ROLE_ADMIN')%}
                        <ul class="navbar-nav text-center">
                              <li class="nav-item ">
                                <a class="nav-link admin" href="{{ path('admin_session_index') }}">{{ "admin la skateshool" | upper }}</a>
                              </li>
                              <li class="nav-item text-center">
                                <a class="nav-link admin" href="{{ path('admin_actu_index') }}">{{ "Admin les news" | upper }}</a>
                              </li>
                              <li class="nav-item text-center">
                                <a class="nav-link admin" href="{{ path('admin_video_index') }}">{{ "Admin les vidéos" | upper }}</a>
                              </li>
                        </ul>
                      {% endif %}
                      {% endif %}
                      <li class="nav-item text-center">
                        <a class="nav-link" href="{{ path('homepage') }}">ACCUEIL</a>
                      </li>
                      <li class="nav-item text-center">
                        <a class="nav-link" href="{{ path('skateschool') }}">LA SKATESCHOOL</a>
                      </li>
                      <li class="nav-item text-center">
                        <a class="nav-link" href="">LES NEWS</a>
                      </li>
                      <li class="nav-item text-center">
                        <a class="nav-link" href="">NOTRE CATALOGUE</a>
                      </li>
                      <li class="nav-item text-center">
                        <a class="nav-link" href="">NOS VIDÉOS</a>
                      </li>
                      <li class="nav-item text-center">
                        <a class="nav-link" href="">LA SÉRIGRAPHIE</a>
                      </li>
                      <li class="nav-item text-center">
                        <a class="nav-link" href="">QUI SOMMES NOUS</a>
                      </li>
                      <li class="nav-item text-center">
                        <a class="nav-link" href="">CONTACT</a>
                      </li>
                    </ul>
                  </div>
                </nav>
              </div>
            </div>
          </div>
{% endblock nav %}
{% endblock %}