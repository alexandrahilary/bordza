{% include 'nav.html.twig' %}

{% block body %}
<div class="container-fluid">
    <div class="row justify-content-center">
    <h1>DÉTAILS</h1>
    </div>
    <div class="row justify-content-center ligne">
    
    <div class="col-7">
        <div class="row justify-content-center">
            
            <div class="col-12"><img class="img-fluid" src="{{ asset('uploads/images/' ~ image.pathImage) }}"></div>
            <div class="col-12 text-center">Alt : {{ image.alt }} / Credit photo : {{ image.creditPhoto }}</div>
            
        </div>
        <div class="bouton">
            <div class="row justify-content-center">
                <button type="submit" class="btn btn-small bouton">
                    <a href="{{ path('admin_image_index') }}">Retour à la liste d'Image</a>
                </button>
                <button type="submit" class="btn btn-small bouton">
                    <a href="{{ path('admin_image_edit', { 'id': image.id }) }}">Modifier les attributs de l'Image</a>
                </button>
                <button type="submit" class="btn btn-small bouton">
                    <a href="{{ path('admin_image_edit_image', { 'id': image.id }) }}">Modifier l'Image</a>
                </button>
            </div>
        </div>
{% endblock %}
