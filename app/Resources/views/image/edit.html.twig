{% include 'nav.html.twig' %}

{% block body %}
<div class="container-fluid identifiant justify-content-center">

    <div class="row justify-content-center">
        <div class="col-12">
            <div class="row justify-content-center">
                <h1>Modifier les attributs de l'image</h1>
            </div>
        </div>
        <div class="col-5 border border-dark" style="padding:20px">
            <div class="col-12">
                <img class="img-fluid col-12" src="{{ asset('uploads/images/' ~ image.pathImage )}}">
            </div>
            {{ form_start(edit_form) }}
                <div class="form">
                    <div class="input-group ">
                        {{ form_label(edit_form.alt, 'alt', {'label_attr' : {'class' : 'input-group-text bg-dark text-light'}}  ) }}
                        {{ form_errors(edit_form.alt) }}
                        {{ form_widget(edit_form.alt, {'attr': {'class':'form-control'}}) }}
                    </div>
                </div>
                <div class="form">
                    <div class="input-group ">
                        {{ form_label(edit_form.creditPhoto, 'credit photo', {'label_attr' : {'class' : 'input-group-text bg-dark text-light'}}   ) }}
                        {{ form_errors(edit_form.creditPhoto) }}
                        {{ form_widget(edit_form.creditPhoto, {'attr': {'class':'form-control'}}) }}
                    </div>
                </div>
                <div class="form bouton">
                    <div class="row justify-content-center">
                        <button class="btn btn-dark"type="submit" value="Edit">Enregistrer les modifications</button>
                        {{ form_end(edit_form) }}
                    </div>
                    <div class="row justify-content-center">
                        {{ form_start(delete_form) }}
                            <button class="btn btn-small" type="submit" value="Delete">Supprimer</button>
                        {{ form_end(delete_form) }}
                    </div>
                    <div class="row justify-content-center">
                        <a href="{{ path('admin_image_index') }}"><- Revenir à la liste d'images</a>
                    </div>
                </div>

    
        
        
    
{% endblock %}
